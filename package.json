{
    "name": "staycool-pos",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
        "build": "npm run build --prefix frontend && npm ci --prefix backend && cd backend && npm exec -- prisma generate --schema prisma/schema.prisma",
        "deploy:vps": "bash deploy-vps.sh",
        "install:all": "npm install --prefix frontend && npm install --prefix backend"
    },
    "devDependencies": {
        "@playwright/test": "^1.61.0",
        "concurrently": "^9.1.0"
    },
    "dependencies": {
        "sharp": "^0.35.3"
    }
}
